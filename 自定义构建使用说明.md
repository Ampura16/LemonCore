# LemonCore `1.21.11-dev` 分支构建与修改说明

仓库地址：

```text
https://github.com/Ampura16/LemonCore
```

以下以 Windows PowerShell 为例，推荐在IDEA集成环境下统一操作。构建前建议准备：

- Git
- JDK 21
- 可用的 GitHub 访问权限
- Windows 使用 PowerShell
- Linux/macOS 将 `gradlew.bat` 替换为 `./gradlew`

## 1. Clone 仓库到本地

执行以下命令，将指定分支和子模块一起克隆到本地：

```powershell
git clone --branch 1.21.11-dev --single-branch --recursive https://github.com/Ampura16/LemonCore.git
cd LemonCore
```

如果仓库是私有仓库，GitHub 可能要求登录或使用 SSH。SSH 方式如下：

```powershell
git clone --branch 1.21.11-dev --single-branch --recursive git@github.com:Ampura16/LemonCore.git
cd LemonCore
```

如果已经 Clone，但子模块没有初始化：

```powershell
git submodule update --init --recursive
```

确认当前分支和远程仓库：

```powershell
git branch --show-current
git remote -v
```

当前分支应为：

```text
1.21.11-dev
```

## 2. 应用 Patch

进入仓库根目录后执行：

```powershell
.\gradlew.bat applyPatches
```

Gradle 任务名称是 `applyPatches`，不是 `applyPatch`。

该命令会根据仓库中的 Paper/Minecraft 补丁，将源码准备到本地工作区。首次执行可能需要下载依赖，因此耗时较长。

执行完成后，可以查看仓库状态：

```powershell
git status
```

如果只是验证仓库能否正常构建，可以执行：

```powershell
.\gradlew.bat :paper-server:createMojmapBundlerJar
```

如果要构建实际用于运行的重新映射版本：

```powershell
.\gradlew.bat :paper-server:createReobfBundlerJar
```

## 3. 修改代码并提交到仓库

### 3.1 修改普通源码

普通的 LemonCore、Paper 或 API 源码通常位于：

```text
paper-server/src/main/
paper-api/src/
```

修改完成后，检查变更：

```powershell
git status
git diff --stat
git diff
```

### 3.2 修改 Minecraft 原版源码

如果修改的是 Minecraft 原版源码，例如：

```text
paper-server/src/minecraft/java/
paper-server/src/minecraft/resources/
```

修改完成后，需要重新生成补丁：

```powershell
.\gradlew.bat rebuildPatches
```

然后再次查看 Git 变更：

```powershell
git status --short
git diff --stat
```

通常应提交重新生成的补丁文件，而不是直接提交 `src/minecraft` 下的工作区源码。具体补丁目录以 `git status` 显示的结果为准。

### 3.3 检查不应提交的文件

提交前不要将以下构建生成内容加入 Git：

```text
.gradle/
build/
paper-server/build/
```

通常也不需要提交生成的 JAR 文件：

```text
paper-server/build/libs/
```

### 3.4 添加修改文件

推荐根据 `git status` 结果精确添加文件：

```powershell
git add <需要提交的文件>
```

例如：

```powershell
git add paper-server/src/main
git add paper-api/src
git add paper-server/src/patches
```

如果确认工作区中没有无关修改，也可以添加全部变更：

```powershell
git add -A
```

检查暂存区内容：

```powershell
git diff --cached --stat
git diff --cached
```

创建提交：

```powershell
git commit -m "Customize LemonCore server"
```

推送到 GitHub 的 `1.21.11-dev` 分支：

```powershell
git push origin HEAD:1.21.11-dev
```

如果本地分支已经正确跟踪远程分支，也可以使用：

```powershell
git push
```

首次推送时，可以建立本地分支与远程分支的跟踪关系：

```powershell
git push -u origin 1.21.11-dev
```

如果仓库是私有仓库，推送时需要使用具有写入权限的 GitHub 账号、Personal Access Token 或已配置的 SSH Key。

## 4. 构建修改后的核心 JAR

### 4.1 修改普通源码后构建

如果只修改了普通源码，可以直接执行：

```powershell
.\gradlew.bat :paper-server:createMojmapBundlerJar
```

### 4.2 修改 Minecraft 源码后构建

如果修改了 Minecraft 原版源码，应先重新生成补丁：

```powershell
.\gradlew.bat rebuildPatches
```

然后构建：

```powershell
.\gradlew.bat :paper-server:createMojmapBundlerJar
```

### 4.3 构建实际运行版本

如果目标是将 JAR 放入实际服务器运行，通常使用：

```powershell
.\gradlew.bat :paper-server:createReobfBundlerJar
```

## 5. 完整操作流程

从 Clone 到构建的完整流程如下：

```powershell
git clone --branch 1.21.11-dev --single-branch --recursive https://github.com/Ampura16/LemonCore.git
cd LemonCore

.\gradlew.bat applyPatches

# 修改源码

# 如果修改了 paper-server/src/minecraft：
.\gradlew.bat rebuildPatches

# 构建开发映射版本
.\gradlew.bat :paper-server:createMojmapBundlerJar

# 或构建实际运行版本
.\gradlew.bat :paper-server:createReobfBundlerJar
```

## 6. 查看构建产物

构建产物通常位于：

```text
paper-server\build\libs\
```
我们通常会使用构建产物`paper-bundler-1.xx.xx-Rx.x-SNAPSHOT-mojmap.jar`开服。  
例如`paper-bundler-1.21.11-R0.1-SNAPSHOT-mojmap`。  
使用 PowerShell 查看最新生成的 JAR：

```powershell
Get-ChildItem .\paper-server\build\libs\*.jar |
    Sort-Object LastWriteTime -Descending
```

## 7. 常用命令对照表

| 操作 | 命令 |
|---|:--|
| Clone 指定分支 | `git clone --branch 1.21.11-dev --single-branch --recursive ...` |
| 初始化子模块 | `git submodule update --init --recursive` |
| 应用补丁 | `.\gradlew.bat applyPatches` |
| Minecraft 源码修改后生成补丁 | `.\gradlew.bat rebuildPatches` |
| 构建 Mojmap JAR | `.\gradlew.bat :paper-server:createMojmapBundlerJar` |
| 构建实际运行版本 | `.\gradlew.bat :paper-server:createReobfBundlerJar` |
| 查看修改 | `git status`、`git diff` |
| 添加修改 | `git add` |
| 创建提交 | `git commit -m "Customize LemonCore server"` |
| 推送到指定分支 | `git push origin HEAD:1.21.11-dev` |

通常提交的是源码、补丁和必要的项目配置，不提交 `paper-server/build/libs/` 中生成的核心 JAR。  

---
**Powered by Ampura16** · **@BlockLand Realms**  
**All rights to the original source code belong to MojangAB and PaperMC.**    

